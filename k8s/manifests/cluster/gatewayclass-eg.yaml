# GatewayClass for Envoy Gateway on Docker Desktop.
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: eg
  labels:
    app.kubernetes.io/part-of: dating-app
spec:
  controllerName: gateway.envoyproxy.io/gatewayclass-controller
